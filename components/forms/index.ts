// Formulaire de contact natif (Pipedrive API + Resend)
export { ContactForm } from './ContactForm';

// Legacy — Pipedrive WebForm embed (deprecated)
export { PipedriveContactForm } from './PipedriveContactForm';
